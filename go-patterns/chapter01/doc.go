package main

//If package comments are large (as is the case with detailed documentation of the formatting options provided
//by the fmt package), then the general convention is to place these comments inside the package in a file
//called "doc.go".
